version: '3.8'
services:
    php80-apache:
        container_name: php80-apache
        build:
            context: .
            dockerfile: Dockerfile.80
        volumes:
            - /home/jesus/proyectos/:/var/www/html/
        extra_hosts:
            - host.docker.internal:host-gateway
        ports:
            - 8080:80
